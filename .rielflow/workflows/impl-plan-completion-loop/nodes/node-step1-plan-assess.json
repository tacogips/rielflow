{
  "id": "step1-plan-assess",
  "description": "Find the next incomplete active implementation plan and report whether active implementation work is complete.",
  "nodeType": "command",
  "variables": {},
  "command": {
    "scriptPath": "scripts/assess-plan.sh",
    "envTemplate": {
      "PLAN_PATH": "{{workflowInput.planPath}}",
      "TARGET_TASKS_JSON": "{{workflowInput.targetTasks}}"
    }
  },
  "output": {
    "description": "Return JSON with plan_complete, activePlanFound, selected planPath, incompleteTasks, completedTasks, designReference, and delegation guidance."
  }
}
