{
  "id": "rielflow-manager",
  "description": "Coordinate the implementation-plan completion loop.",
  "executionBackend": "codex-agent",
  "model": "gpt-5.5",
  "promptTemplateFile": "prompts/rielflow-manager.md",
  "variables": {},
  "input": {
    "description": "Workflow arguments. Optional `workflowInput.planPath` can constrain execution to one active implementation plan; otherwise the workflow selects the next incomplete plan from impl-plans/active. Optional `workflowInput.maxIterations` can document the operator's intended loop budget.",
    "jsonSchema": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "workflowInput": {
          "type": "object",
          "additionalProperties": true,
          "properties": {
            "planPath": {
              "type": "string",
              "minLength": 1
            },
            "maxIterations": {
              "type": "number",
              "minimum": 1
            }
          }
        }
      }
    }
  },
  "output": {
    "description": "Return JSON preserving the target plan path and execution constraints for downstream steps."
  }
}
