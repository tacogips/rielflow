This is the first visit to the reusable worker node.

Echo the normalized request into a field like `echoText` and explain that the
workflow should revisit the shared node one more time through its dedicated
answer step.

Latest inbox payload:
{{inbox.latest.output}}
