Normalize the incoming request for the reusable worker node.

Return a concise JSON payload with fields such as:
- `requestText`
- `instruction`
- `expectedSecondTurn`

Latest inbox payload:
{{inbox.latest.output}}
