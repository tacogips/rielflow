{
  "id": "route-message",
  "description": "Select exactly one initial persona for the incoming Discord message. Yui is the default when no persona is explicitly named.",
  "executionBackend": "codex-agent",
  "model": "gpt-5.4-mini",
  "promptTemplateFile": "prompts/route-message.md",
  "variables": {},
  "output": {
    "description": "Return payload.target plus adapter-level when flags used by transition labels.",
    "jsonSchema": {
      "type": "object",
      "required": ["target"],
      "properties": {
        "target": {
          "type": "string",
          "enum": ["yui", "mika", "rina"]
        },
        "reason": {
          "type": "string"
        }
      },
      "additionalProperties": true
    },
    "maxValidationAttempts": 2
  }
}
