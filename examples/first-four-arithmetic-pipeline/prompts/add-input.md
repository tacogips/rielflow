Read the latest human input.

Parse space-separated numbers from the request. Require at least four numeric values. Return:
- `allNumbers`: every parsed number in input order
- `firstFour`: exactly the first four numbers
- `operands`: the first two numbers only
- `ignoredTail`: any remaining numbers after the first four
- `stage`: `add-input`
