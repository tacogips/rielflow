Use the latest upstream payload from `add-input`.

Add the two numbers in `operands` and return a JSON object with:
- `operation`: `add`
- `operands`: the two input numbers
- `result`: the numeric sum
