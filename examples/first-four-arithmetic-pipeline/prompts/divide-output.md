Assemble the final result from the latest `divide-input` and `divide-worker` outputs.

Return:
- `firstFour`: the same four selected numbers
- `finalResult`: the division result
- `summary`: a short string describing `(n1 + n2) * n3 / n4`
- `operations`: preserve earlier operations and append the division step
