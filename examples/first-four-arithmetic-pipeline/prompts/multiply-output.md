Assemble the multiplication-stage handoff from the latest `multiply-input` and `multiply-worker` outputs.

Return:
- `stage`: `multiply-output`
- `firstFour`: the same four selected numbers
- `result`: the multiplication result
- `nextOperand`: the fourth number from `firstFour`
- `operations`: preserve earlier operations and append the multiplication step
