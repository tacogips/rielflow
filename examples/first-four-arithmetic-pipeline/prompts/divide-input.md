Read the latest upstream handoff from `multiply-output`.

Return:
- `stage`: `divide-input`
- `firstFour`: the same four selected numbers
- `operands`: an array of exactly two numbers:
  first element is the prior `result`
  second element is `nextOperand`
