Coordinate `node-combinations-showcase` as a small authoring reference.
Start each eligible plain lane independently, and keep the loop-body lane
focused on repeated iteration rather than a separate foreach schema.
