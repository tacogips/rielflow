Coordinate `node-combinations-showcase` as a small authoring reference.
Start each eligible plain lane independently, and run the foreach lane as an
explicit step graph: the judge step chooses labeled transitions until the lane
exits to the final output step.
