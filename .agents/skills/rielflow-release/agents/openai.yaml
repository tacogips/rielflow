interface:
  display_name: "Rielflow Release"
  short_description: "Release rielflow through Homebrew tap assets"
  default_prompt: "Use $rielflow-release to prepare, publish, and verify this rielflow Homebrew release."
